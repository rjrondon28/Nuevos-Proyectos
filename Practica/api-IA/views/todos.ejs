<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>Estudiantes Registrados</h2>
    <ul>
        <% estudiantes.forEach(estudiante => { %>
            <li>
                <%= estudiante.nombre %> - <%= estudiante.email %>
                <form action="estudiantes" method="PUT" style="display:inline;">
                    <input type="hidden" name="id" value="<%= estudiante.id %>">
                    <input type="text" name="nombre" placeholder="Nuevo nombre" required>
                    <input type="email" name="email" placeholder="Nuevo email" required>
                    <button type="submit">Actualizar</button>
                </form>
                <form action="estudiantes/<%= estudiante.id %>" method="DELETE" style="display:inline;">
                    <button type="submit">Eliminar</button>
                </form>
            </li>
        <% }); %>
    </ul>
</body>
</html>